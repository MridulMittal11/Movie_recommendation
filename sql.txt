create database movie_recommendation;

use movie_recommendation;

create teable users(
userid varchar(255) primary key,
password varchar(255)
);

insert into users values ('himanshu', 'himanshu'), ('sanskar', 'sanskar');